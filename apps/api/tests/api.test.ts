import {describe,it,expect} from 'vitest'; import request from 'supertest'; import {app} from '../src/server';
describe('API',()=>{it('health',async()=>expect((await request(app).get('/health')).body).toEqual({ok:true}));it('rejects unauthenticated tools',async()=>expect((await request(app).get('/tools')).status).toBe(401));it('validates classify auth',async()=>expect((await request(app).post('/classify').send({description:'x'})).status).toBe(401));it('validates registration payload before database access',async()=>expect((await request(app).post('/auth/register').send({email:'not-an-email',password:'short'})).status).toBe(400));it('handles CORS preflight',async()=>expect((await request(app).options('/tools')).status).toBe(204))});
